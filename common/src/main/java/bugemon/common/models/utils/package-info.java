/**
 * General-purpose utility value types shared across the model layer.
 *
 * <p>
 * Currently contains {@link bugemon.common.models.utils.Position}, an immutable grid coordinate record used by the
 * tower floor map and the floor-map view.
 */
package bugemon.common.models.utils;
