/**
 * Combat screen views. All concrete classes share a single {@code Combat.fxml} layout loaded by the abstract
 * {@link bugemon.client.views.combat.CombatView}; each subclass implements {@code initCombatMode()} to show or hide
 * mode-specific regions (action menu, team-switcher).
 */
package bugemon.client.views.combat;
