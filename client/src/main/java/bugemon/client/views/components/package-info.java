/**
 * Reusable JavaFX custom components shared across multiple screens. Each class extends
 * {@link bugemon.client.views.components.ComponentView} and loads its own FXML via {@code fx:root}, allowing it to be
 * embedded directly in parent FXML files or instantiated programmatically.
 */
package bugemon.client.views.components;
