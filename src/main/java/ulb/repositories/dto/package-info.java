/**
 * Plain record DTOs used to carry raw database rows between the repository and the service layer. These are internal to
 * the persistence layer — callers above the service layer should use model objects instead.
 */
package ulb.repositories.dto;
