/**
 * Checked exceptions thrown by {@link ulb.services.game.TeamService} for team persistence failures (duplicate name,
 * unknown name). Checked so that callers are forced to handle them explicitly.
 */
package ulb.services.exceptions;
