/**
 * Persistence DTOs: plain records that carry raw data between the repository and service layers. Never exposed above
 * the service layer — callers should use domain model objects instead.
 */
package ulb.common.dto.persistence;
