/**
 * Reusable sub-components embedded inside the combat screen. Each class is a self-contained
 * {@link ulb.views.components.ComponentView} that manages one panel of the combat UI — action selection, attack list,
 * item inventory, or Bugemon switching.
 */
package ulb.views.combat.components;
