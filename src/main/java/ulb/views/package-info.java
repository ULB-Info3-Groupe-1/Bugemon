/**
 * JavaFX view layer. Each class represents one screen or reusable UI component; FXML layouts are loaded eagerly at
 * construction time.
 *
 * Views never access the model directly — all data flows through the controller as DTOs, keeping views decoupled from
 * model internals.
 */
package ulb.views;
