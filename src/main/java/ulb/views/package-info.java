/**
 * JavaFX view layer. Each class represents one screen or reusable UI component.
 *
 * Views never access the model directly — data flows in via setters, and user interactions flow out through a
 * {@code Listener} interface implemented by the controller.
 */
package ulb.views;
