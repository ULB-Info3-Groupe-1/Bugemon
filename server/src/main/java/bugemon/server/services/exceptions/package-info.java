/**
 * Checked exceptions thrown by {@link bugemon.server.services.TeamService} for team persistence failures (duplicate
 * name, unknown name). Checked so that callers are forced to handle them explicitly.
 */
package bugemon.server.services.exceptions;
